export type rbFilialsType = {
    id: number
    title: string,
    state: number,
    performed: number
}

export type rbQuarrysType = {
    id: number
    title: string,
    volume: number,
    extractedVolume: number
}
